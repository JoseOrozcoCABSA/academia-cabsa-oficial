/**
 * Rutas de la aplicacion en un solo sitio, para no repetir cadenas en los
 * enlaces.
 *
 * Es solo un mapa de nombres: la tabla de rutas real esta en
 * `routes/AppRoutes.jsx`, y cambiar una aqui **no** cambia la otra. Hay que
 * mantener las dos alineadas.
 */
export const ROUTES = { home: '/', courses: '/cursos', library: '/mediateca', assistants: '/asistentes', progress: '/progreso' };
