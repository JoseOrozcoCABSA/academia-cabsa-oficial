/**
 * @file Pantalla `App`.
 *
 * Envoltorio sobre `AppRoutes`: fija sus propiedades y no añade lógica.
 *
 * El archivo está escrito en una sola línea en el original.
 *
 * @see @/routes/AppRoutes Implementación.
 */

import AppRoutes from '@/routes/AppRoutes'; export default function App(){ return <AppRoutes />; }
