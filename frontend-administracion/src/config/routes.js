/**
 * Rutas del panel en un solo sitio, para no repetir cadenas en los enlaces.
 *
 * Es solo un mapa de nombres: la tabla real esta en `routes/AppRoutes.jsx` y no
 * se sincroniza sola con este archivo.
 */
export const ROUTES={home:'/',academia:'/academia',content:'/contenido',ai:'/ia',analytics:'/analitica',users:'/usuarios',notifications:'/notificaciones'};
