/**
 * @file Reexporta `Input` desde el barril de componentes comunes.
 *
 * Existe para poder importar `Input` por su propia ruta además de
 * hacerlo desde `@/components/common/index`. No añade comportamiento.
 */

export { Input as default, Input } from '@/components/common/index';
